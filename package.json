{
  "name": "tokenmagic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/build-ui.mjs",
    "watch": "tsc --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "typescript": "^5.9.2"
  }
}
